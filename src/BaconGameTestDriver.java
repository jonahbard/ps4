public class BaconGameTestDriver {

    private static void runTests(boolean useSmallTestData) {
        BaconGraph bg = new BaconGraph(useSmallTestData);
        bg.
    }

    public static void main(String[] args) {
        runTests(true);
        runTests(false);
    }

}
